import React from "react";
import ReactDOM from "react-dom/client";
import App from "./routes/get_start";
import Start  from "./routes/start";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Start/>
  </React.StrictMode>,
);
